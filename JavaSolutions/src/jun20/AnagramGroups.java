package jun20;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

public class AnagramGroups {
    public List<List<String>> groupAnagrams(String[] strs) {
//        if (strs.length == 0 || strs == null) {
//            return new ArrayList<>(Arrays.asList(new ArrayList<>(Arrays.asList(new String[]{""}))));
//        } // directly return [[""]] if strs is empty or null.
//
//        List<char[]> sort = new ArrayList<>(); // create a list to store the sorted element, for grouping.
//
//        for (int i = 0; i < strs.length; i++) {
//            char[] c = strs[i].toCharArray();
//            sort.add(i,c);
//        }
//        return 0;
        return null;
    }
}
